meta {
  name: Delete a project leader
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/projectleider/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
