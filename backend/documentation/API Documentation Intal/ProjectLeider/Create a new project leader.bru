meta {
  name: Create a new project leader
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/projectleider
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "Name": ""
  }
}
