meta {
  name: Delete a permission group
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/permissions/group/:id
  body: none
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{token}}
}
